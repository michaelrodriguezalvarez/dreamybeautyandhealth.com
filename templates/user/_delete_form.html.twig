<form method="post" action="{{ path('user_delete', {'id': user.usuario.id, 'role':role}) }}" onsubmit="return confirm('¿Desea eliminar este usuario?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.usuario.id) }}">
    <button id="btn_delete" class="btn btn-danger m-1">Eliminar</button>
</form>
