{% extends 'base.html.twig' %}

{% block title %}Beneficio{% endblock %}

{% block body %}
    <h1>Beneficio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ beneficio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ beneficio.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ beneficio.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('beneficio_index') }}">back to list</a>

    <a href="{{ path('beneficio_edit', {'id': beneficio.id}) }}">edit</a>

    {{ include('beneficio/_delete_form.html.twig') }}
{% endblock %}
