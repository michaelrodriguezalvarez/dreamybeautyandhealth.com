<form method="post" action="{{ path('beneficio_delete', {'id': beneficio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beneficio.id) }}">
    <button id="btn_delete" class="btn btn-danger m-1">Eliminar</button>
</form>
